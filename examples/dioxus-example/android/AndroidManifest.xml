<?xml version="1.0" encoding="utf-8"?>
<!--
  Merged into the manifest `dx` generates. Holds the permissions whose
  attributes `[android.permissions]` in Dioxus.toml cannot express.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Declaring that the scan is not used to derive a location means android
         does not ask for the location permission on top of it. -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="s" />
    <uses-permission android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />

    <uses-feature
        android:name="android.hardware.bluetooth_le"
        android:required="true" />
</manifest>
